def slugify(text: str) -> str:
    """Convert text to a URL slug.

    Lowercase; every run of characters that are not a-z or 0-9 becomes a single "-";
    no leading or trailing "-".
    """
    raise NotImplementedError
